# File-Security-Tool

This is a tool that is used to determine whether files in a network have been modified, deleted, or stayed the same.  This tool uses JSON and hashlib to hash a file and compare it to previous renditions of that file to determin if it has been modified or not. 
